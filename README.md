```go
package main

import (
	"fmt"
)

func main() {
	info := make(map[string]string)
	info["Name"] = "Yahor"
	info["Surname"] = "Maksimchyk"
	info["Technologies"] = "Go, JS, Node.js"

	fmt.Println(info)
}

```
